# Main configuration for Hydra
# Self-Verified Efficient Consistency (SVEC) prompt tuning experiment

defaults:
  - _self_
  - run: ???  # loads from config/run/, accessible as 'run=' in CLI

# Execution mode: main, sanity_check, or pilot
mode: main

# Results output directory
results_dir: .research/results

# WandB configuration
wandb:
  entity: a
  project: a
  mode: online

# Cache directory for datasets and models
cache_dir: .cache
